import { WorkAbstract } from "./workAbstract.js";

export default class sample extends WorkAbstract {
    constructor(ctx, canvasWidth, canvasHeight) {
        super(ctx, canvasWidth, canvasHeight);
    }

    init() {
    }

    update() {
        super.update();
    }

    draw() {
    }

    touchMoved(touch) {
    }
}